/**
 * Card variants define the visual style of a card component. Each variant has its
 * own set of design tokens that control aspects like background color, border
 * color, and elevation.
 */
export type CardVariant = 'elevated' | 'filled' | 'outlined';
